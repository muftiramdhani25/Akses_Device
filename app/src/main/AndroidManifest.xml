<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aksesdevice">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <uses-permission android:name="android.permission.CALL_PHONE" />

    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.Wifi"></activity>
        <activity android:name=".activity.Ttd" />
        <activity android:name=".activity.Telepon" />
        <activity android:name=".activity.Sms" />
        <activity android:name=".activity.Email" />
        <activity android:name=".activity.Camera" />
        <activity android:name=".activity.Browser" />
        <activity android:name=".activity.Bluetooth" />
        <activity android:name=".activity.AudioRec" />
        <activity android:name=".activity.AudioManagerActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>